# Tavern Bash (repo: bazaar-brawler)

A mobile auto-battler set in a Persian night market. The display name became Tavern Bash in 0.38.1; the repo, URLs, and internal ids keep the old name. 0.53.0 was the clean hands pass, scrubbing names that collided with other games' properties (all internal ids unchanged, deviations recorded in the parity test's rename ledgers): Bazaar Crossbow to Souk Crossbow, Bazaar Rats to Souk Rats, the Venomancer charm to Poisonmonger (Dota hero name), enchants Fiery and Icy to Blazing and Frosted (The Bazaar's enchant names), the sandstorm to the simoom everywhere players see it, and the display words Trinkets and Anomaly to Charms and Omen (both are Hearthstone Battlegrounds seasonal mechanic names). Flagged and unresolved: the title Tavern Bash sits close to Hearthstone's Tavern Brawl and Bob's Tavern; Robbie is choosing whether to rename. This repo is self-contained and independent of Robbie's second-brain vault; nothing here should be written there, and nothing there is needed here.

## Source of truth

Read `handoff-bazaar-brawler-2026-07-09.md` in full before making changes, then `handoff-bazaar-brawler-2026-07-10.md` for the decisions that supersede it. It carries the rules spec, data tables, the phased mission, the art direction, and the decisions not to re-litigate. Phases 0 through 4 are fully complete: module structure, tests, PWA, saves, Netlify deploy, the painted-art pipeline with all 50 assets live (26 items, 8 monsters, 8 portraits, rarity frames wired as 9-slice borders, board, background), Pixi particles, synthesized sound effects, and the two Suno music loops. Phase 5, content, is underway. The four engine-ready backlog monsters have shipped, one per version: Scalded Samovar 0.7.0, Shahmaran 0.8.0, Marid of the Cistern 0.9.0, Nasnas 0.10.0. Along the way: unique wares (Serpent Crown, Tide Wall; `unique:true` in `ITEMS`, filtered out of the shop roll and rival generation so fight parity holds; the parity test allows new monsters and unique wares while keeping everything the original shipped byte-identical) and the Fusion Mote bounty (a free bronze copy of your commonest ware). New monsters still await painted art; their SVG medallions show until PNGs land in `public/art/monsters/`. Regen shipped in 0.11.0 (a `regen` field on a side knits fight health each second via `healSide`, capped at the starting pool, no cleanse, never resurrects; monsters declare it in data and gild scales it) along with Ghul Matron. 0.12.0 added the per-fight storm override (`stormAt` in seconds on a monster def, wired through `startFight` opts) plus Sandling and the Weeping Stone, the first player-side regen ware (`regen` on an item def, summed by `boardRegen` in the engine, rarity-scaled, unique so rivals and parity never see it). 0.13.0 added the bounty-drain tick (a `pocket` field on a monster board item; the engine tallies grabs on `F.pocketed` and emits `pocket` events, the market settles the bounty afterward) plus Pilfer Monkey. 0.14.0 shipped freeze and flying, one slot in the settled system order: `fx.freeze` on a monster item pauses the leftmost intact enemy ware's timer (freeze and thaw events, `.frz` cell tint), and `flying` items cannot be struck by weapons (with only flyers left, weapons hit the merchant; a flying bulwark cannot taunt). The Icebox carries them, bounty is the Flying Charm (`adjFly`: neighbors fly, locked at fight start like all adjacency). 0.15.0 shipped crit behind Glass Peri: a `crit` chance on a fight item doubles that swing's damage, rolled from the fight's seeded rng so replays stay deterministic (items without crit never draw, so old fights keep their rng stream and parity holds). Her bounty is the Glass Prism (`critAll`: a board-wide 20% crit aura for weapons, unique). 0.16.0 shipped on-destroy: a `rattle` on an item def fires when the item dies, `spawn` replaces it in place at the same index with fresh timers, and `selfdestruct:true` makes an item's own activation its death, which is how the Roc Egg's 15 s hatch works (band 3; its bounty is the Roc Egg ware, a passive unique that hatches a fighter for you when cracked). Netlify deploys are blocked on account credits (r.blinkman@gmail.com); versions stack locally, one commit each, single deploy planned at v1. 0.17.0 shipped haste-all (`fx.hasteAll` quickens every other item on its side, never itself) with Simurgh Fledgling; its bounty is the Simurgh Feather, a self-flying unique weapon. 0.18.0 shipped ammo (`ammo:N` on an item def is a magazine: one round per activation, an empty magazine holds its swing without accumulating, `fx.reload` tops up every own magazine) with Mint Golem, whose bounty is the pair of uniques, Coin Cannon and Coin Hopper. 0.19.0 opened The Dragon Gate: band 4 doors at rounds 10 and up, 8 health loss chip, and The Azhdaha behind them (three 50 Integrity heads; a `hasteMates` rattle verb permanently cuts the survivors' cooldowns when one dies; its bounty is the Azhdaha Fang, which rages your own board when it shatters). 0.20.0 added the Night Auctioneer and the auction (`fx.disable` puts the finest enemy weapon under the hammer, inert but targetable for the rest of the fight; a `pay` field on the caller compensates the player win or lose; the player's Gavel unique carries the same disable). 0.21.0 completed the backlog with the Grand Vizier of Ash: 700 HP behind a full ten slot board (Ash Bulwark, Cinder Core burn, Frost Scepter freeze, Ash Chalice heals), bounty is the vault, a pick-any-unique overlay (`bounty:{pickUnique:true}`, `openUniquePick` in ui.js). The whole design-doc bestiary is in: 23 monsters across four bands, 11 unique wares, every backlog system except enchantments, the Vault economy, and heroes, which stay post-v1. The 1.0.0 bump was walked back as premature; Phase 5 then completed its last three systems, co-designed with Robbie and approved 2026-07-10: 0.23.0 enchantments (`ENCH` in data.js: six prefixes riding existing keywords; shop cards roll enchanted 12% at a 3 gold premium; enchants ride fusion and saves, rivals never roll them), 0.24.0 the Vault (three off-board stash slots; stored wares neither fight, earn, nor forge; gold stays unbankable), 0.25.0 heroes (`HEROES` in data.js: pick one of four at run start; personal tag weights your shop x2.2; mods merge through `computeT`, `firstFlat` is the Knifegrinder's leftmost bonus; hero glyph replaces p-0). The v1 art batch landed (76 painted assets, every slot filled; hero portraits live at `portraits/h-*.png` and flow through the manifest untouched). The presentation roadmap 0.27 to 0.31, co-designed with Robbie against Hearthstone BG, The Bazaar, and Underlords, shipped in one deploy: 0.27 the acts (one slim ribbon with standings, anomaly, and trinket tap drawers; Market and Doors as tabbed stages; the stall docked at the bottom with the Vault behind a flip and To Battle always in reach; both orientations pin to viewport; a dawn title card opens every round), 0.28 the scene (wooden counter with lantern pooling, arched doors, drifting backdrop), 0.29 the juice (buy flight, sell coins, tier flare, income coins, press states), 0.30 the duel (large versus portraits, attacker to victim streaks, magnitude scaled numbers), 0.31 the voice (per hero bark lines in `HEROES[].barks`, triggered on forge, broke, monster win or loss, and first boss sight, throttled in `bark()`). A second research driven pass, 0.32 to 0.35, closed the gaps Robbie's competitive study of Battlegrounds, The Bazaar, and Underlords surfaced: 0.32 the telegraph (pairings roll in `pairRound()` at round start and ride the save as indexes; the duel button wears the opponent), 0.33 the pulse (live conic cooldown rings on fight cells painted from sim state, frost when frozen, dust when dry), 0.34 the tempo (a persisted 1x or 2x combat speed toggle, `FSPD` times the tick feed), 0.35 the reveal (anomaly card with doubled rays and an 88px glowing icon; enchanted buys land with a burst in the enchant color). Note for future sessions: the dev preview tab freezes rAF and CSS transitions, so fight visuals and streaks need on-device eyes. 0.26.0 was the presentation pass: overlay cards and the scout panel wear the painted gold frame as a border-image, the hero picker shows large portraits with colored tag lines, section labels carry a brass diamond, and fights are now visually distinct from the draft (a Dusk Falls title sweep on fight start, `body.fight` dims and desaturates the market backdrop and ember drift, and a fixed `.battleframe` layer glows crimson at the screen edges). Still owed before v1: Robbie's on-device playthrough for pacing and look, and the production deploy once the original account's Netlify credits return. Two Netlify sites exist: the real one, https://bazaar-brawler.netlify.app (account r.blinkman@gmail.com, linked via `.netlify/state.json`, frozen at 0.15.0 until credits are added) and a test site, https://bazaar-brawler-v1.netlify.app (account robcdownie@gmail.com, site id 0acdf55f-ebf9-42b8-b8b7-f6da13e45e48, currently 0.26.0). Deploy to the test site with `npx netlify deploy --prod --dir dist --site 0acdf55f-ebf9-42b8-b8b7-f6da13e45e48 --no-build` while the CLI is logged into the robcdownie account; never relink the repo away from the real site. Painted art batches can land any time: drop PNGs into `public/art/` per the naming rules in `scripts/make-art-manifest.js`, run `npm run art`, and the game picks them up. The 2026-07-11 session shipped 0.38.1 through 0.46.0 from Robbie's requested-changes PDF plus the reflection-notes.md diagnosis: the Tavern Bash rename; the gate pass (the doors leave their tab and bar the way to the duel via `openGate`, round 1 opens on the market, monster kills pay at least 1 gold, gold spoils queue on `G.spoils` and land with dawn income since they would burn at dusk otherwise); the triple telegraph (a held pair of same id and rarity glimmers, a shop card that completes a triple wears a Forges Silver chip; fusion always matched across enchants, the missing piece was the signal); market freeze (`G.frozen`, one button, frozen paid cards count against the next roll, reroll thaws); the vault sheet (tap to select, To the Stall, Swap trades directly with a board ware so full vault plus full board never deadlocks, Sell); the tuning pass approved from the balance harness (serpent poison 3 to 2, vial cd 3 to 3.5, venom 7 to 5, genRival budget 8+(round-1)*6, Nasnas to band 3; `tests/parity.test.js` now carries a REBALANCED_ITEMS ledger, pins combat parity on fixed boards of untouched items, and everything outside the ledger stays byte-identical); the drama pass (overlay cards sit on the painted market via --card-art, dawn and dusk cards hold 2.2 s, a photo finish with both merchants low plays at half speed); the toolbelt (?debug pins a strip with build tag, PWA or TAB, viewport, fps, and overflow count; `node scripts/ship.js <kind> "subject"` runs tests, bumps, commits); and the front door (Robbie's painted title screen as `bg/bg_intro.png` with stone-plaque New Game and Tutorial buttons cut from the art, his painted icon at 512/192/180, CACHE_V bb-v25). `node scripts/balance-sim.js` prints seeded winrate, damage-share, and pacing readouts; run it before and after any tuning. Still owed: the tutorial build (the intro button is stubbed with a toast), doors panel art, Robbie's on-device playthrough (have him add ?debug), and the v1 deploy decision: top up the real site's credits or promote the test site. The 2026-07-11/12 session continued through 0.54.3, all shipped to the test site (the deploy cadence is now: stack versions locally, one commit each via `node scripts/ship.js`, deploy on Robbie's word, not per version). 0.47.0 the threshold pass (Robbie's two painted door portraits at `ui/door_monster.png` and `ui/door_safe.png`, each gate card sits on its portrait behind a darkening gradient). 0.48.0 the tutorial (the intro Tutorial plaque starts a real run in guided mode: a `.coach` card walks seven steps on `G.tut`, hero pick to a round-2 handoff, each skippable, done flag in `bb-tut`; steps with `ov:true` float above overlays). 0.48.1/0.48.2/0.48.3 the portrait then crest then shelf passes (fights fill the viewport; the ribbon became one carved bar of label-over-value chips; the market became a carved panel with docked controls, two-up shop in portrait, slack distributes between rows and excess scrolls inside the shelf so the stall never leaves the screen). 0.49.0 the legibility pass (synthesis of two adversarial UI reviews: gold and health lead the bar, the anomaly never truncates, the effect chip leads each card, size pips gained a slots caption, Reroll wears a coin and Freeze a frost mark, the stall's next slot glows and an empty board reads "your wares fight from here", the duel button reads To Battle over the opponent name). 0.50.0 the forge pass (four playtest notes: `fuseWithVault` pulls vaulted copies through to complete a triple on buy, return, or gild, and the shop shine counts vault copies; two Gold copies forge Diamond via `fuseNeed`, a recorded rebalance with engine tests; Dusk Falls holds the sim 1.5 s; the place chip drops the /8; plus the landscape draft regression fix where the dead tabs row stopped the stage scrolling). 0.51.0 the sidebar pass (landscape puts the crest bar on the left as a rail with the hero's painted portrait on top; the manifest declares `orientation:landscape` and a `#rotate` curtain covers portrait since iOS ignores the lock). 0.52.0 the ledger pass (Copy Run Report on both end screens writes a paste-ready block: version, result, hero, tier, health, gold and pending spoils, anomaly and tags, stall with rarities and enchants, vault, trinkets, the door record on `G.stats`, shrine use; a selectable-text fallback panel if the clipboard refuses). 0.53.0 the clean hands pass (the name scrub, see the top paragraph). 0.54.0 the score pass (Robbie's nine Suno tracks in `public/music`, matched by lowercased filename token: title and boss loops joined market and battle, plus one-shot stingers dawnsting, forgesting, windstorm, fanfarewin, lament through a new `sting()` channel in music.js; title plays on the intro and hands to market at round one). 0.54.1/0.54.2 the marquee then wide-intro fixes (the portrait intro painting could not show both nameplate and dragon in a landscape crop, so Robbie generated a true 16:9 title painting, `bg/bg_intro_wide.png`: landscape fits it whole with the enlarged stone buttons on its painted plaques, portrait keeps the tall painting). 0.54.3 the fight-and-sound fixes (three from device screenshots: a landscape rule was re-showing the rail during fights so the hero portrait overlapped the enemy health bar, now `body.fight` hides the rail and reclaims its grid column; the door cards dropped the 44px arch that clipped their health text since painted doors carry their own arch; and the music loop was rewritten from overlapping crossfade sources to a single `loop=true` source per track that hard-stops on switch, because iOS would not stop the orphaned market sources and the draft loop bled into fights). The art-batch skill (`.claude/skills/art-batch`) and `scripts/art-prep.js` now own the art round trip; `playthrough-checklist.md` is Robbie's on-device pass. The test site https://bazaar-brawler-v1.netlify.app is current (0.54.3, CACHE_V bb-v28); the real site https://bazaar-brawler.netlify.app is still frozen and unlinked from the deploy path. Voice tracks are still owed (the `bark()` voice channel is wired to accept them, prompt sheet with exact hero lines is in the session log). App Store path discussed 2026-07-12: wrap the PWA with Capacitor (bundles offline, passes Apple's minimum-functionality bar), needs the $99/year Apple Developer Program and a Mac on macOS Sonoma+ to submit (Robbie's 2015 Mac may cap too low; cloud Mac or CI is the fallback). The name decision should land before any store submission. Offline note: the service worker is cache-first but only precaches four files at install; everything else (JS chunks, art PNGs, audio) caches lazily on first fetch, so a full offline experience requires visiting the screens once online first; a failed offline fetch of the lazy Pixi chunk is caught in fx.js and degrades to no particles rather than crashing, and missing audio is a silent no-op.

## Layout

| Path | What it is |
|---|---|
| `bazaar-brawler.html` | The original single-file game. Never edit it; the parity test in `tests/parity.test.js` checks the modules against it fight for fight. |
| `index.html` | The page: CSS, SVG sprite, markup. Loads `src/main.js`. |
| `src/engine.js` | The combat sim. Pure, deterministic, DOM-free, headless-testable. Keep it that way. |
| `src/data.js` | Items, monsters, trinkets, anomalies, personas, constants. |
| `src/ui.js` | All DOM rendering and game flow, including the saves system. |
| `src/fx.js` | The Pixi particle canvas: hit sparks, ember drift, storm sand, forge burst, coin rain, 40+ flash. Pixi loads lazily as its own chunk; every effect is a no-op under reduced motion or without WebGL. |
| `src/sfx.js` | All sound, synthesized with raw WebAudio (no audio files). Unlocks on first tap for iOS; mute persists under bb-mute; every call is a no-op before unlock. |
| `src/music.js` | Crossfade-looping WebAudio player for the Suno tracks in `public/music/` (`market.mp3` for draft, `battle.mp3` for fights). Lazy-loads after first tap, obeys the mute toggle, total no-op with no files. |
| `public/music/` | Suno tracks drop here; the ingest script files any audio named with a market or battle token. |
| `src/art.js` | Sprite access point. Renders the painted PNG for an id when the manifest has one, else the SVG symbol. Same svg wrapper both ways, so CSS never changes. |
| `src/art-manifest.js` | Generated map of glyph id to painted PNG path. Never edit by hand; `npm run art` rebuilds it (also runs before dev, build, and test). |
| `public/art/` | Painted art drops here: `items/{id}.png`, `monsters/{glyph suffix}.png` (the Debt Collector is `debt.png`), `portraits/p0.png` to `p7.png`, plus `frames`, `board`, `bg`. All 50 slots are filled. Rarity frames render as 9-slice border-image overlays on cells and ware cards via the `art-frames` class that `applyBigArt` sets. |
| `public/` | Manifest, service worker, generated icons. Bump `CACHE_V` in `sw.js` and `SAVE_VERSION` in `ui.js` when a deploy must invalidate old state. |
| `scripts/make-icons.js` | Regenerates the placeholder icon set. |
| `scripts/make-art-manifest.js` | Scans `public/art` and writes `src/art-manifest.js`. Filename rules are documented at the top of the file. |

## Commands

- `npm test` runs the full suite: engine behavior, parity against the original file, dash scan, sprite symbol coverage, art manifest sync, ingest matcher. Any refactor that breaks it reverts.
- `npm run ingest -- <folder> [--strip-bg]` files raw image generations into `public/art`: matches each file by an id token in its name, resizes to spec, converts to PNG, optionally flood-fills a solid background to transparent, and regenerates the manifest. Unmatched or ambiguous files are reported and left alone.
- `npm run dev` serves on the port in `.claude/launch.json` (5199).
- `npm run build` then `npx netlify deploy --prod --dir dist` ships to https://bazaar-brawler.netlify.app (site already linked via `.netlify/state.json`).

## Hard rules

- Zero em dashes and zero en dashes anywhere, including docs, comments, and commit messages. The dash scan in `npm test` enforces this and it scans markdown, so this file counts.
- Approved change batches get applied in full, not piecemeal.
- Target device is an iPhone in Safari standalone mode, landscape first: test at 844x390 (landscape is the primary orientation, per Robbie on 2026-07-10, superseding the handoff), then 390x844. Respect safe areas, no hover-dependent interactions. In landscape runs the app pins to viewport height and the doors and market columns scroll internally; nothing may push the stall off screen.
- One new system per version. Each phase ships working, tests green, with the live Netlify URL shown at the end.
- The sim stays pure and the two health layers, targeting rules, and economy in the handoff are settled; do not re-litigate them.

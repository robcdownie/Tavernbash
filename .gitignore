node_modules/
dist/
.netlify/

# Local cloud credentials and Vite overrides
.env
.env.*
!.env.example
!.env.analytics.example

# Full-resolution painted originals; the shipped copies in public/art are
# the dieted WebP/quantized derivatives (scripts/art-diet.js)
art-source/

# Playwright layout tests
test-results/
playwright-report/
debug.log

# Screenshot harness output (scripts/shots.mjs); regenerate with npm run shots
shots/

# Codex working files, never committed (per the R7 handoff)
.agents/
.tmp/
